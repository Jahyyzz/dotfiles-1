alt + Escape
    pkill -USR1 -x sxhkd

alt + shift + a
    firefox

alt + q
    qutebrowser

alt + shift + b
    brave-browser-stable

alt + shift + l
    libreoffice

alt + shift + r 
    rofi -show run

alt + shift + e
    ./.local/scripts/dmenu/edit_configs.sh

alt + shift + y
    ./.local/scripts/ytsubs.sh

alt + shift + c
    ./.local/scripts/dmenu/herbst-logout.sh

XF86AudioMute
    volchange.sh volmute

XF86AudioRaiseVolume
    volchange.sh volup

XF86AudioLowerVolume
    volchange.sh voldown


